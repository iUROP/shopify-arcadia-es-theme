<div class="i-announcement-bar Bg-Colors-100 announcement-slick">
  {% for block in section.blocks %}
    <li class="i-announcement-bar__item Text-Colors-200 Typo-Body-S Family-Font---Body">
      {% if block.settings.show_subscribe %}
        <a href="/products/miel-cruda-de-azahar">
      {% endif %}

      {{ block.settings.text }}

      {% if block.settings.show_subscribe %}
        </a>
      {% endif %}
    </li>
  {% endfor %}
</div>

{% schema %}
{
  "name": "iUROP Announcement Bar",
  "tag": "section",
  "class": "section",
  "settings": [],
  "blocks": [
    {
      "type": "content_block",
      "name": "Item",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Texto"
        },
        {
          "type": "checkbox",
          "id": "show_subscribe",
          "default": false,
          "label": "Mostrar CTA de suscripción"
        },
      ]
    }
  ],
  "presets": [
    {
      "name": "iUROP Announcement Bar"
    }
  ]
}
{% endschema %}
